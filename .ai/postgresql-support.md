# PostgreSQL support

Working plan for PostgreSQL support. Tick a line when its commit lands; the
tables below are the only place that says where we are.

## Where we are (2026-09-23)

- **Stage 1 is done**: 15 commits on `postgresql-support`, PR #214.
- **Stage 2 is done**, stacked on it in five PRs (see the table below): the
  dialect, the driver, the connection, the catalog and connection errors, then
  the editor, the UI and the docs. PostgreSQL is a choosable engine; every
  claim of the table below was measured on `tiana-dev-postgres`.
- `yarn lint`, `yarn knip` and `yarn test` are clean at every commit. Three
  files were already off prettier before this work and are not ours to
  reformat: `installSource.test.ts`, `FreeTextFilterModal.stories.tsx`,
  `splitStatements.ts`.
- What is left is listed under "Open decisions" and "Known issues".

Stage 1 commits, in order: `ac33984` engine · `c241482` dialect · `b7fba55`
plan · `f65192a` literals · `fda4e75` **fix** foreign key link · `ee517b6`
driver · `c5410dd` `WriteResult` · `5e187fe` `FieldKind` · `b1ee37f` **fix**
cells · `99515ca` metadata · `53f4f72` guarded update · `264789c` SQL errors ·
`b34c6d4` parser per engine · `9f095b4` dialect contract · `a754138` **fix**
port.

## Why

The app only speaks MySQL/MariaDB, through `mysql2` in the main process. The
README has long promised PostgreSQL "one day", on the assumption that "every
query the app sends is standard SQL". The code says otherwise: `SHOW DATABASES`,
`SHOW TABLE STATUS`, `SHOW KEYS`, `USE`, the backquotes of `escapeIdentifier`,
the `<=>` operator, `LIMIT 1` on an `UPDATE`, `CAST(… AS JSON)` — and above all
the MySQL wire-protocol type numbers (`Types` from `mysql` v2) that decide how
every cell is rendered, right inside the renderer.

Three MySQL bugs sit on the way and get fixed in passing: a `TIME`, `YEAR`,
`BIT` or `GEOMETRY` column blanks the grid (`Cell.tsx` throws in its `default`),
so does a real `BLOB` (React refuses to render a `Buffer`), and
`ForeignKeyLink` builds `col="value"` without escaping the value.

## Decisions

1. **The UI's "database" level is the PostgreSQL schema.** A PG connection
   carries a database (a new form field, default `postgres`); the database list
   lists its schemas. `db.table` becomes `schema.table`, which stays legal, and
   `information_schema` keyed on `TABLE_SCHEMA` too. No routing, context or
   `configByDatabase` change.
2. **Two stages.** Stage 1 is a neutral abstraction: MySQL stays the only
   engine, tests green, nothing visible changes beyond the three named `fix:`
   commits. Stage 2 plugs the PG driver into seams already proven.
3. **Full PG scope**: browsing, grid, filters, foreign keys, structure, SQL
   editor with completion, charts, and cell editing with conflict detection.
4. **An engine is a driver and a dialect, not a product.** MariaDB is `MySQL`:
   same wire protocol, same driver, same quoting. What does set them apart the
   server declares itself — it is never asked of the user.
5. **The configuration file is not validated and stays that way.** It is parsed
   with a cast, as before. A missing `engine` is decided in `loadConfiguration`;
   no migration, `version` is not bumped. Test fixtures describe real files —
   none names an engine — so they carry `@ts-expect-error`, not a type of their
   own.

## Target architecture

The structuring split:

- **`src/sql/dialect/`** — SQL text and escaping, **no driver import**, so the
  renderer can load it (it builds `SELECT * FROM db.table`, the `WHERE` filter
  prefix, and the `USE`).
- **`src/sql/driver/`** — connecting, executing, translating field types. Main
  process only, the sole place `mysql2` and `pg` are imported.

```
src/sql/
  engine.ts            DatabaseEngine
  resultField.ts       FieldKind, ResultField
  types.ts             ConnectionObject, QueryReturnType, WriteResult, ResultRow
  sqlError.ts          SqlErrorData, asSqlError, isSqlError (tag `kind: 'sql'`)
  connectionError.ts   asConnectionError, classifyConnectionError
  tableStructure.ts    structure page: columns + references, the 9 fields
  updateCell.ts        UpdateCellRequest / UpdateCellOutcome (IPC contract)
  dialect/
    index.ts           getDialect(engine) — DIALECTS: Record<DatabaseEngine, …>
    types.ts           Dialect, GuardedUpdate
    readQuery.ts       BuiltQuery, ReadQuery, readQuery() (zod parse inside)
    metadata.ts        DialectMetadata, ForeignKey, ColumnDetail, DescribedColumn
    dialect.contract.test.ts   the rules every dialect is held to
    mysql/     index.ts escapeIdentifier.ts metadata.ts guardedUpdate.ts
               dataType.ts columnSemantics.ts
    postgres/  (stage 2, same files)
  driver/
    index.ts           Driver, DriverConnection, loadDriver() — IMPORTS record
    mysql/     index.ts (asServerError, toResultFields) fieldKind.ts
    postgres/  (stage 2) + namedPlaceholders.ts catalogCache.ts
  parser/
    index.ts           getParser(engine) — PARSERS record, collectEntities
src/renderer/component/MonacoEditor/
  language.ts          engine ↔ Monaco language — LANGUAGES record
```

### What the renderer stops knowing

The IPC contract goes from `[rows, FieldPacket[]]` to `[rows, ResultField[]]`:

```ts
export enum FieldKind {
  Number,
  Date,
  DateTime,
  Time,
  Boolean,
  String, // base0B, today's colour for VARCHAR/CHAR
  Text, // base05, today's colour for TEXT/BLOB/ENUM/SET
  Json,
  Binary,
  Array,
  Unknown,
}
export interface ResultField {
  name: string;
  table: string | null;
  kind: FieldKind;
}
```

`String` and `Text` are two members because the current rendering colours them
differently; merging them would be a visible change. `Enum` is not a member: the
MySQL protocol announces an `ENUM` column as a string, and `resolveEditorKind`
already decides from the schema.

**A cell renders in three tiers, in this order**: the shape of the value first
(`null`, `Buffer`/`Uint8Array`, non-`Date` object), then the `FieldKind`, then a
`String(value)` fallback. `TEXT` and `BLOB` share one MySQL protocol type, so no
`FieldKind` can separate them — and this is what closes the two grid crashes.

Also leaving the renderer: the 8 `import { Types } from 'mysql'`, the four
copies of the numeric-type `Set`, `escape` from `mysql`, `src/sql/dataType.ts`
and the `enum('a','b')` parser.

### Metadata shapes

They become resolved facts rather than server vocabulary, because the labels of
a PG enum type live nowhere in `information_schema` (they are in `pg_enum`), so
the renderer cannot derive them.

```ts
// src/sql/dialect/metadata.ts — each answer is a ReadQuery: SQL, bound values,
// a zod row schema and a `read`; ConnectionStack.#answer runs and parses it
interface DialectMetadata {
  listDatabases(): ReadQuery<string[]>;            // the schemas, on PG
  listTables(db): ReadQuery<string[]>;             // tables and views
  listForeignKeys(db): ReadQuery<ForeignKey[]>;    // FK columns only, pairs kept
  listPrimaryKeyColumns(db, t): ReadQuery<string[]>; // in key order
  listColumns(db): ReadQuery<ColumnDetail[]>;
  describeTable(db, t): ReadQuery<DescribedColumn[]>; // structure page, no References
}

interface ForeignKey { table; column; referencedTable; referencedColumn }
interface ColumnDetail {
  table: string; name: string;
  nullable: boolean; generated: boolean; binary: boolean; json: boolean;
  allowedValues: string[];  // empty when the column is not a closed set
  multiValued: boolean;     // a MySQL `SET`
}
// keys are the page's column heads: Column Type Null Key Default Extra
// Collation Comment (+ References, joined in neutral code)
interface DescribedColumn { … }
```

Database and table names are **sorted in `ConnectionStack`**, by code unit: a
dialect answers them in any order (a server collation would put `user_role`
after `users`, and differ per engine).

`SHOW TABLE STATUS` also returned `Rows`, `Data_length` and `Comment`: **no
consumer** (checked — fixtures only). They are not carried over, which spares us
emulating `reltuples`/`pg_table_size` on PG.

### The guarded write

`<=>` does not exist on PG, nor does `LIMIT 1` on an `UPDATE`, and `json` there
has **no equality operator** — hence a `CAST(… AS jsonb)` on **both sides** of
the guard, which also gives the semantics we want (key order stops mattering).
`CAST(x AS jsonb)` and not `::jsonb`: the named-parameter rewriter would read
`:jsonb` as a parameter.

`RETURNING` changes the conversation: on PG a write that matches returns the
row, so `guardMatches` — which only exists to lift the ambiguity of MySQL's
`affectedRows` — becomes useless, and editing a cell costs one query instead of
two.

So the dialect owns the **interpretation** as well as the text, and `updateCell`
keeps no engine branch:

```ts
// src/sql/dialect/types.ts — Dialect.guardedUpdate(request): GuardedUpdate
interface GuardedUpdate<Read = unknown> {
  write: BuiltQuery;
  outcomeOfWrite(written: QueryReturnType): UpdateCellOutcome | undefined; // MySQL: always undefined
  readBack: ReadQuery<Read>; // its shape is the dialect's own
  outcomeOfReadBack(written, read: Read): UpdateCellOutcome;
}
// MySQL reads { value, guardMatches } | undefined, PostgreSQL { value } | undefined
```

`updateCell` is: send `write`, ask `outcomeOfWrite`, and only when it answers
`undefined` run `readBack` through `#answer` and ask `outcomeOfReadBack`. On PG
the write's `RETURNING` settles the common case in one query; the read-back is
only needed to tell a conflict from a deleted row.

### Errors

`isSqlError` tests `'code' in e && 'errno' in e`: a `pg` error has a SQLSTATE
`code` but **no `errno`**, so it would be rejected and would reach the route's
ErrorBoundary instead of the result tab. The sniffing is removed rather than
widened: the driver classifies at the source and the error carries a
`kind: 'sql'` tag, exactly as `asConnectionError` already does. Same for the
"connection is in closed state" retry, a mysql2 message, which becomes
`driver.isConnectionLost(error)`.

`type-guard.ts` sniffs `ResultSetHeader` keys (`fieldCount`, `serverStatus`…):
an `INSERT` on PG would display nothing in the raw SQL page. It is replaced by a
`WriteResult` with an explicit discriminant.

Both done (commits 6 and 11). A PG driver must therefore: answer `WriteResult`
for a write (`rowCount` → `affectedRows`, `insertId: null`), tag a refused
statement with `asSqlError(error, { code: SQLSTATE })` (no `errno`: the result
tab then prints the code alone), and implement `isConnectionLost` on its
connection.

## Stage 1 — neutral abstraction, MySQL only

Every commit leaves `yarn lint && yarn test` green. The `refactor:` ones change
nothing visible; the three `fix:` ones are isolated and each carries its test.

| #   | Status   | Commit                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| --- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **done** | `DatabaseEngine`, `ConnectionObject.engine`, default decided in `loadConfiguration`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2   | **done** | `src/sql/dialect/`: `Dialect`, `getDialect()`, MySQL dialect. `escapeIdentifier.ts` moves in, `qualify()` and `useDatabase()` join it. The 7 callers switch: `#dialect()` in the main process, a `useDialect()` hook in the renderer, `loadDialect()` for the SQL page action, which is not a component. `buildUpdateCellQuery` and `filterClause` keep importing `escapeIdentifier` straight until commits 10 and 3 move them in. _(Merged with what was commit 3: a dialect module no caller imports is a knip finding.)_                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 3   | **done** | `dialect.escapeLiteral` and `booleanLiteral`. `cellValueToSqlLiteral` and `CellContextMenu` stop importing `mysql`, and `buildFilterClause` takes the dialect. `escapeLiteral` is typed on a string: `escape` takes `any`, and its object branch calls `Buffer.isBuffer` — undefined in the renderer — while its `Date` branch shifts the value into a time zone it never had                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 4   | **done** | **`fix`** `ForeignKeyLink` builds its filter through `buildFilterClause`/`cellValueToSqlLiteral`; the dialect reaches it as a prop from `TableGrid.columnsMeta`, since a hook there would cost two `useContext` per cell. Visible: `?where=col="42"` becomes `` `col` = 42 ``, and a key holding NULL no longer offers a link to a row it never pointed at                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 5   | **done** | `src/sql/driver/`: MySQL driver extracted from `#connect`/`#executeQuery`, `loadDriver()` carries the lazy import and memoises it, the `end`/`error` listeners go behind `onClosed`, and the retry goes through `isConnectionLost` — on the _connection_ rather than the driver, since only a query sent down an open socket can lose it. The driver is handed named parameters instead of a spread of the stored connection, so no field of ours reaches it by accident. Decryption stays in the stack, where CLAUDE.md documents it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 6   | **done** | `WriteResult`, `ResultRow`, `type-guard.ts` deleted. A result is rows or a write summary, and `Array.isArray` separates them — no key of a driver's own is read. `RowDataPacket` leaves the renderer with it. **IPC contract changed — restart the app**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 7   | **done** | `FieldKind` and `ResultField`, mapping in `driver/mysql/fieldKind.ts` written **from `Types`**, never from bare numbers, held to mysql2's own list by two exhaustiveness tests — and the list is the **name → number** half of its map, the reverse one being short of `VECTOR`. `QueryResult<T> = [T, ResultField[]]`, so the renderer imports neither `mysql` nor `mysql2` any more. 33 files. Two named visible changes: a `YEAR` column is answered as a number (measured), so it renders, charts and edits as one instead of blanking the grid; and `table` is read from `orgTable`, so a foreign key link, a column detail and an editor now resolve on an aliased query (`FROM commandes c` answered `table="c"`, `orgTable="commandes"`) — it is also the neutral reading, since `pg` resolves a relation OID to the real name and never to an alias. **Restart the app**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 8   | **done** | **`fix`** `Cell.tsx` renders in three tiers — the shape of the value, then the kind, then `String(value)` — so no value can blank the grid again. Visible: a `TIME` column shows `HH:MM:SS`, a `BIT`, `BLOB` or `VARBINARY` column the hexadecimal literal of its bytes (`0x89504E47…`, cut at 150 bytes in the grid and 4096 in the modal, where it used to read `{"0":137,"1":80,…}`), and a `GEOMETRY` column the `{x,y}` it is answered as. The kind is read second and not first because it cannot be read first: `TEXT` and `BLOB` are one wire type, and a `CAST` carries no collation to correct it with. The collation does correct a declared column, and now does: measured, 63 on a text kind is what separates `BLOB` from `TEXT`, `VARBINARY` from `VARCHAR` and `BINARY` from `CHAR` — asked of a text kind only, since `TIME`, `BIT` and `GEOMETRY` all carry 63 without holding characters, and read from the collation and not the flags, MariaDB's `JSON` carrying `BINARY_FLAG` over a string. Not visible today, the value tier catching the same bytes, but it makes the kind true of an empty result — and PostgreSQL will answer the same from the `bytea` OID. Editability stays schema-decided: `GEOMETRY` is bytes there and `Unknown` here                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 8b  | todo     | **`fix`** _(optional, low priority — stored procedures are not a goal today)_ `toQueryReturn` tells a stored procedure's answer from rows. Measured on `tiana-dev-mysql`: `CALL p()` answers `[RowDataPacket[], ResultSetHeader]` where `SELECT` answers `RowDataPacket[]`, and mysql2 declares both — today the `CALL` reaches the grid as two nonsense rows, as it already did through `isRowDataPacketArray`. The discriminant stays key-free: the first element is an array while the last is not. A `CALL` is one statement, so the SQL editor sends it. Visible: `CALL p()` shows its first result set. It belongs in `driver/mysql/`, not in the dialect — it is about what the socket answered, not about SQL text, and PostgreSQL has no equivalent shape                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 8d  | **done** | `build` **zod** becomes a runtime dependency of the main process, for the boundary below. Measured: `z.array(row).parse` costs 1.83 ms on 20 000 rows, and the 3 593 columns of every schema of `tiana-dev-mysql` parse in 36 ms round trips included — nothing next to the socket. Verified with a real `yarn package` that it inlines into `main.js` (360 KB) with no `node_modules` shipped                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 9   | **done** | The 6 metadata questions move into `dialect/mysql/metadata.ts`, and **no identifier is interpolated any more**: the three `SHOW` forms named their subject in the statement itself, so ``SHOW TABLE STATUS FROM `db` `` and ``SHOW KEYS FROM `db`.`t` `` became `INFORMATION_SCHEMA.TABLES` and `INFORMATION_SCHEMA.STATISTICS` with the name bound. Verified against `tiana-dev-mysql` that each pair answers the same rows, views and a composite key included. The **order** is not the server's: `INFORMATION_SCHEMA`'s collation puts `user_role` after `users` (seen on `mysql` and `sys`, where `tiana_dev` hid it), so `ConnectionStack` sorts database and table names by code unit — identical to `SHOW` on every schema but `information_schema`, which `SHOW` does not sort at all — and the rule holds for every engine. A question is **declarative**: `metadataQuery(name, { sql, values, row, read })` puts the SQL, the values it binds, the zod schema of a row and the reading of the answer side by side, and is the only way a dialect writes one — so the rows are always parsed, in one place, before `read` sees them. The dialect runs nothing: `ConnectionStack.#answer` sends it and calls `answer(rows)`, and the dialect tests are synchronous. An injected runner was tried first and dropped once zod was in: its only extra power, several statements per question, is not needed — PostgreSQL's enum labels fit one query (`array_agg` over `pg_enum`, to be measured), and composing questions (`describeTable` + `listForeignKeys`) happens in neutral code. The reading is code and never the `AS` of a `SELECT`: an alias is a string nothing checks against the type it fills, and PostgreSQL folds an unquoted one to lower case, so `AS referencedTable` would answer `referencedtable` and the field would read `undefined` on a type promising a string. What a server answers is **parsed and not cast**: a zod schema per statement, the row type derived from it with `z.infer`, so the two cannot drift, and a failure naming the question, the row and the column instead of an `undefined` the renderer discovers by drawing an empty cell. It holds only what the server said — what a dialect then builds from it is ours and the compiler owns it. The last cast of the path goes too: `#answer` refuses a write summary through `isWriteResult` rather than asserting it away, which leaves `rows as T` in `executeQueryAndRetry` as the only one left, where it was deliberately put in commit 6. `describeTable` is a question like the others: it reads the bare columns of `INFORMATION_SCHEMA.COLUMNS`, no alias and no correlated `GROUP_CONCAT`. `References` is computed in neutral code (`src/sql/tableStructure.ts`) from `listForeignKeys`, so a new engine only describes its columns, and the page's nine fields are declared with the kinds MySQL used to announce (`table: null`: `'COLUMNS'` would have looked links up in a user table of that name). Compared on every table of `tiana-dev-mysql` with the old query: 326 tables, 3 593 columns, identical. `ColumnDetail` is now facts (`nullable`, `binary`, `json`, `generated`, `allowedValues`, `multiValued`) and no server vocabulary, so `dataType.ts` and the column semantics are MySQL's own business again. `executeQueryAndRetry` splits in two: `#send` retries and throws, the public one wraps it in the `{ result, error }` envelope IPC needs. **Restart the app** |
| 10  | **done** | `GuardedUpdate`: `buildUpdateCellQuery.ts` moves to `dialect/mysql/guardedUpdate.ts` (git sees a rename) with its SQL unchanged, and the dialect now owns the **interpretation** too — `outcomeOfWrite` (settled by the write alone, `undefined` on MySQL, `RETURNING` on PostgreSQL) and `outcomeOfReadBack`. `updateCell` has no engine branch left: write, ask the write, read back only when it did not settle. The read-back is a `ReadQuery` parsed with zod, `CellReadRow` gives way to a neutral `CellRead` (`guardMatches` a boolean), and `MetadataQuery` is renamed `ReadQuery` in its own `dialect/readQuery.ts` since it now serves two uses. MySQL's decision table — the one that was untested — is tested on hand-written answers, each branch caught by a mutation, and replayed on a scratch table of `tiana-dev-mysql`: updated, same value rewritten, changed, forced, deleted, to and from `NULL`. **Restart the app**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 11  | **done** | `SqlErrorData` (`code`, `errno?`, `sqlState?`) in `src/sql/sqlError.ts`, which replaces `isSqlError.ts`. The driver tags a refused statement **at the source** with `asSqlError`, as `asConnectionError` already did, so `isSqlError` reads the `kind` tag instead of sniffing `code` and `errno` — a `pg` error has no `errno` and would have reached the ErrorBoundary. The MySQL driver tags exactly what the old sniff accepted (a `code` and an `errno`), so nothing changes there; measured through the real driver on MariaDB 11.8: `ER_BAD_FIELD_ERROR`, `ER_PARSE_ERROR`, `ER_NO_SUCH_TABLE` all arrive decoded as SQL errors. `encodeError` loses its sniffing branch. `sql` and `sqlMessage` are dropped: nothing read them. `SqlErrorComponent` prints `errno: code`, or the code alone when there is no number (PostgreSQL)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 12  | **done** | `src/sql/parser/index.ts` (moved from `mysqlParser.ts`): `getParser(engine)`, one shared instance per engine, and `collectEntities(parser, sql)`. `SqlParser` is the four methods the app calls, so dt-sql-parser's `PostgreSQL` plugs in as is. `splitStatements`, `hasLimitClause`, `extractTableAliases` and `analyzeQuery` take the engine. `Dialect.engine` carries it to the renderer, which already holds a dialect; the parser stays **out** of the dialect, which the main process imports too and would then bundle ANTLR. `MonacoEditor/language.ts` maps engine and Monaco language both ways: completion, validation and semantic tokens register for every language of the map and read the engine off the model's language, so a JSON model gets none of them. `RawSqlEditor` takes `engine` as a prop (the SQL page and `WhereFilter` pass it; a prop keeps the stories free of a connection) and reads it through a ref, like its callbacks — switching the language of a live editor is commit 23. `MARKER_OWNER` → `'sql-syntax'`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 13  | **done** | `dialect/dialect.contract.test.ts`, a `describe.each` over every `DatabaseEngine`, so the PostgreSQL dialect gets checked for free. On each of the 11 statements a dialect writes with bound values (the 6 metadata questions, the guarded write guarded, forced, on a composite key and on a JSON column, and the read-back): it binds **exactly** the parameters it names, it is one statement, it never interpolates a value it binds. And `escapeIdentifier` refuses the empty string, doubles its own quote character and keeps a dotted name whole; `qualify` quotes each part on its own; `useDatabase` is one statement naming the database quoted; `booleanLiteral` tells true from false. Each rule was checked by breaking it in the MySQL dialect. The copies of these invariants in the MySQL tests are gone; what stays there is MySQL's own text                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 14  | **done** | **`fix`** the port was a string while its type said `number`: the form's `<Input>` answers text. It is an `InputNumber` now (an integer from 1 to 65535, no spinner, so it looks as before), tested through a real form submit — the old field fails the test with `"3307"`. **Existing files hold a string**, so `loadConfiguration` reads the port with `Number()`, next to the default engine: no migration, `version` not bumped, and the file is rewritten as a number on its next save                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |

## Stage 2 — PostgreSQL

**Sequencing constraint.** `DatabaseEngine.PostgreSQL` cannot land alone: four
records are typed `Record<DatabaseEngine, …>` and stop compiling the moment the
member exists — `DIALECTS` (`dialect/index.ts`), `IMPORTS`
(`driver/index.ts`), `PARSERS` (`parser/index.ts`) and `LANGUAGES`
(`MonacoEditor/language.ts`). Hence the order below: the dialect and the driver
are written and tested **standalone** first (their own test files import them
directly), and one commit then adds the member and registers all four. That is
also the commit where `dialect.contract.test.ts` — which iterates
`Object.values(DatabaseEngine)` — starts holding the PG dialect to the rules,
for free. The order was 15 → 27 in the first draft of this plan; it is
renumbered, same content.

| #   | Status   | Commit                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| --- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 15  | **done** | _(no code)_ A persistent `tiana-dev-postgres` container (PostgreSQL 18, named volume, no `--rm`, `--restart unless-stopped`, port 15432, `postgres`/`devpassword`, database `tiana_dev`), the connection named with the `(dev)` marker. Verification schema in `.ai/postgres-dev-schema.sql`, replayable: a **composite** foreign key, a foreign key **to another schema**, an enum type, a `json` **and** a `jsonb` column, a `STORED GENERATED` column, an `identity` column, a column with a comment and one without, a never-`ANALYZE`d table, `text[]`, `bytea`, a view, a materialised view, a partitioned table, an identifier holding a `"` and a `.`, two schemas whose names sort differently by collation and by code unit                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 16  | **done** | _(merged into 19)_ `pg` and `@types/pg` come with the driver that imports them — alone they are a knip finding. The interop was settled with a throwaway test: under Vitest `import pg from 'pg'` resolves `esm/index.mjs`, whose default export carries `Client`, `types` and `DatabaseError`; the same default import is what the CommonJS build hands back. `pg` is only reachable from `main.js` once the engine is registered, so the bundle is proven in 20                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 17  | **done** | `driver/postgres/namedPlaceholders.ts`: `:name` → `$n`, a repeated name reusing the same `$n`, a missing one throwing. **One regular expression** (after review): only the dialect's own statements are rewritten — the editor's SQL travels as written — and their text holds literals and escaped identifiers around the placeholders, nothing else; a quoted run is copied as it is, and the second colon of a `::` cast is left alone. The first version also stepped over comments, dollar-quoted bodies and `E''` escapes, which it never sees. Only for queries with `values` (`namedPlaceholders: statement.values !== undefined` on MySQL)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 18  | **done** | `dialect/postgres/` (standalone, not registered): `escape.ts` (both functions **copied** from `pg`'s `lib/utils.js`, which cannot be imported: it loads `pg-types`, whose `postgres-bytea` reads `Buffer` on import — measured in the renderer; the one addition is refusing an empty identifier, as the contract requires), the 6 metadata questions over `pg_catalog` (lower-case aliases only where two columns share a name), `guardedUpdate` (`IS NOT DISTINCT FROM`, `CAST(… AS jsonb)` on both sides of the guard only — the new value is bound untyped and parsed as the column's type —, `RETURNING`, no bound). The dialect object itself (`engine`, `qualify`, `useDatabase`, `booleanLiteral`) is assembled in 20, since `engine` needs the enum member. `primaryKeyClause` is shared by both dialects (`dialect/primaryKeyClause.ts`), and `GuardedUpdate<Read>` makes the read-back's shape the dialect's own: MySQL's `CellRead` with `guardMatches`, PostgreSQL's bare `{ value }`, since a write that matched returned the row and one that did not is a conflict whatever the cell holds. **Measured** on every schema of `tiana-dev-postgres` through `pg` and the rewriter of 17 (6 schemas, 224 relations, 2 160 columns, all parsed by zod): `a.attnum = ANY (i.indkey)` works on an `int2vector`; `array_agg(enumlabel)` is a `name[]` that `pg` leaves as text, hence `CAST(… AS text)`; the composite key of `shipments` pairs right. The guarded write replayed on a scratch schema: updated, same value, changed, forced, deleted, forced on a deleted row, to and from `NULL`, `json` and `jsonb` with the keys in another order, enum, `numeric`, `date`. **Decided**: a foreign key to another schema is left out (see Open decisions)                                                                       |
| 19  | **done** | `driver/postgres/` (standalone, not registered): `pg.Client` with the database as an `invariant`, the rewriter of 17 only for statements with `values`, `rowMode: 'array'` when asked, and **`queryMode: 'extended'` always** — the simple protocol would run `SELECT 1; SELECT 2` as two statements, the extended one refuses it (measured: `42601 cannot insert multiple commands into a prepared statement`), which is what `multipleStatements: false` gives on MySQL; a `CREATE FUNCTION … $$ …; $$` still passes. A statement that selects columns answers rows (`UPDATE … RETURNING` included, even with none), any other `{ affectedRows: rowCount ?? 0, insertId: null }`. OID → `FieldKind` keyed by `pg.types.builtins`' names, checked against them at runtime; arrays, enums and domains read `Unknown` until 21. An `interval` is kept as the server spells it. A refused statement is a `DatabaseError`, tagged with its SQLSTATE as `code` (no `errno`). Measured through the real driver: the lost-connection messages (`… is not queryable` after a `pg_terminate_backend` or an `end()`), the connection timeout (`timeout expired` with no code, tagged `ETIMEDOUT`, against a port that accepts and never answers), and the codes of a bad password (`28P01`), unknown database (`3D000`), refused port and unknown host. `ConnectionParams.database` is optional, MySQL ignoring it                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 20  | **done** | `feat` **The engine becomes choosable — first working PG connection.** `DatabaseEngine.PostgreSQL` and its four registrations (`postgresDialect`, assembled here, the lazy driver import, `new PostgreSQL()` from dt-sql-parser, `LanguageIdEnum.PG` with the `pgsql` contribution imported statically next to `mysql`'s). `useDatabase` is `SET search_path TO "s"`, the schema alone: a name found in no other schema must not resolve to `public` behind the user's back. `ConnectionObject.database` (optional, passed to the driver). Form: a `block` `Segmented` with no label as the leading group, the engine names coming from one ICU `select` (`connection.engine.name`, product names identical in both locales); the `database` field under host/port, rendered for PostgreSQL only (default `postgres`) — `preserve={false}` turned out redundant, an unmounted field is not submitted (checked by mutation); port/user follow the engine only while they hold the other engine's default. `dialect.contract.test.ts` holds the PG dialect to the common rules with no change. The routing test of `src/sql/index.test.ts` gains a PG connection (markers `pg_catalog.*`, placeholders numbered). **Bundle**: a real `yarn package` builds with no `external` at all — `pg` lands in a lazy chunk of its own (86 KB), `pg-native` becomes a stub that only throws if `pg.native` is read, and the Cloudflare branch is dropped. **In the app** (CDP, isolated profile): the form, the schema list sorted by code unit, the `users` grid (arrays, `bytea`, `json`, `jsonb`, enum, `numeric`, `time`, `timestamptz`), the structure page, and through IPC on a scratch schema an edit, a conflict, a `jsonb` edit, an `INSERT`, a refused statement (`42703`, shown by code alone) and a two-statement query refused (`42601`) |
| 21  | **done** | Two commits. **Decoding**: `pg` decodes only what the app reads as something else than text — booleans, numbers, `Date`s, JSON objects, `Buffer`s, as mysql2 answers them — and leaves the rest as PostgreSQL spells it (`{math,poetry}`, `1 day 02:00:00`, `(1,2)`): its array, interval and point objects rendered as JSON, and the editor would have written that JSON back, a malformed array literal. **Relation names** (`relationNames.ts`, per connection): `pg` gives a column its table's OID and no name, and the SQL page's foreign-key links and column details look tables up by name (the table page already sets `table` itself). One `pg_class` lookup per unseen OID, `null` remembered for one that names nothing, a failed lookup costing the table and never the result. Measured: an aliased join answers the real names, a view its own, an expression `null`. The planned OID → `FieldKind` for user types is **dropped**: enums, domains and arrays arrive as text and render as text, and the editor takes an enum's labels from the schema                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 22  | **done** | `connectionError.ts` reads PostgreSQL's SQLSTATE, which `pg` puts in `code`: `28P01` (a wrong password — or user, PostgreSQL says the same) and `28000` (a role that may not log in) are `accessDenied`; two new reasons, `unknownDatabase` (`3D000`, PostgreSQL only since MySQL opens no database on connecting) and `tooManyConnections` (`53300`, and MySQL's `ER_CON_COUNT_ERROR` / `ER_USER_LIMIT_REACHED`). Each measured through the real driver with scratch roles and a scratch MariaDB user, dropped after: a role with `CONNECTION LIMIT 0`, a `NOLOGIN` role, a user with `MAX_USER_CONNECTIONS 1`. `08001` / `08004` left out: never produced on connecting here, so unmeasured. Locales EN then FR                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 23  | **done** | **Measured, and the premise was wrong**: `SqlPage` does unmount when the connection changes. The `connections/:connectionSlug` loader redirects any entry into another connection to its active database or table (`expectedUrl`), so the editor is created anew with the right language, and `RawSqlEditor` never sees its `engine` change — the `setModelLanguage` effect, the engine-keyed split cache and the re-validation on `onDidChangeLanguage` were written, tested, then dropped as code for a case that cannot happen; the prop's docblock says so. Checked in the app through CDP: `SELECT 1::int` clean under `pgsql`, `SELEC 1` underlined (a first reading showed a stale marker, left over by modules the hot reload had replaced — a full reload is part of any editor measurement). What remains: the words PostgreSQL reserves beyond SQL:2023 and MySQL join the list `generateTableAlias` avoids                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 24  | **done** | The engine in the connection list meta, through the same ICU `select` as the form (`PostgreSQL · postgres@localhost:5432/shop`, the database a PostgreSQL connection opens after a `/`), and in the `title` of the connection's tab. **Not** in the tab text: its colour is already the prod/dev mark, and doubling every tab's text breaks the 44px floor. Tested on a rendered list and nav, each line caught by a mutation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 25  | **done** | Docs: README (the tagline and "Database support"), CLAUDE.md (an "SQL engines" section on the dialect / driver / parser split and the schema-as-database decision, `pg` in the library table, the single-statement rule on both drivers, the editor's two languages, and the "import `Types` from `mysql`" gotcha replaced by "the drivers are main-process only"), the `sql-editor` skill (two languages, the console listener, the `pgsql` tokenizer colouring quoted identifiers as strings — read from its grammar —, reloading before measuring), `.ai/lessons.md`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |

Rough size, estimated before starting: ~3 500 lines of diff (3 000–4 500),
half of it tests, almost only additions. Most likely to overrun: the catalog
queries (18, none run yet), the rewriter's edge cases (17), the Monaco `pgsql`
integration (20, 23). Two PRs make sense: 15–20 (a PG connection that opens
and shows a table, ~1 700 lines), then 21–25.

## Open decisions

To settle before or while writing the PG dialect:

- **`ForeignKey` has no referenced schema** (same as `main`): a key to
  `other_schema.users` would link to `users` of the current schema. **Settled
  in 18 for PostgreSQL**: such a key is left out of `listForeignKeys`, a
  missing link being better than a wrong one. The MySQL dialect still lists
  them (rare there); `referencedSchema` remains possible later.
- **`DescribedColumn.Comment` is a `string`**: settled in 18, the PG dialect
  answers `COALESCE(col_description(…), '')`.
- **`DescribedColumn` speaks MySQL** (`Key` = PRI/UNI/MUL, `Extra`): PG
  synthesises `Key` from `pg_index` and fills `Extra` with `identity` /
  `generated … stored` or `null`. Named behaviour gap in the PR.
- **Showing the failing SQL** of a metadata error on the connection error page
  (`sql?` in `SqlErrorData`, collapsible detail): not done, it was never shown
  before either. Separate commit if wanted.
- **MariaDB `JSON` columns** read `json: false` (they are `LONGTEXT` +
  `CHECK (json_valid(col))`, only visible in `INFORMATION_SCHEMA.CHECK_CONSTRAINTS`).
  Detecting them would add `CAST(… AS JSON)` to the guarded write, which
  MariaDB rejects (ERROR 1064): the MySQL dialect would first need to know
  MariaDB from MySQL. The editor already detects JSON from the value.

## Known issues left out of stage 1

Already in `main`, found on the way, each worth its own `fix`:

- **MySQL 8 `DEFAULT_GENERATED`**: since 8.0.13, a `created_at TIMESTAMP DEFAULT
CURRENT_TIMESTAMP` column has `EXTRA = 'DEFAULT_GENERATED'`, and
  `isGenerated` (`/GENERATED/i`, `dialect/mysql/columnSemantics.ts`) makes it
  uneditable. Fix: `/(VIRTUAL|STORED) GENERATED/i`. Unverified (no MySQL 8
  container), documented behaviour.
- **Spatial subtypes** (`point`, `linestring`, `polygon`, `multi*`,
  `geometrycollection`) are missing from `BINARY_DATA_TYPES`: editable as text
  and filterable on their bytes.
- **Table-name case** on `lower_case_table_names=2` (macOS MySQL): the
  structure page's `References` compares names with `===` in JS where the old
  SQL join used the server's collation; could come back empty. Not reproduced.
- **zod makes metadata loading all-or-nothing**: one row of an unexpected shape
  fails the whole connection loader, with a clear message. No MySQL/MariaDB
  column found that breaks it; a MySQL 8 smoke run is still owed.
- 8b above (a `CALL` answering two result sets).

## How this work is done

Rules the user set during stage 1, to keep:

- **Never commit without being told to**, in that very message. Staging is
  fine; a review is walked hunk by hunk in a coherent order (contract →
  implementation → main process → IPC → renderer → fixtures → docs), staging
  each group on approval.
- Answers in French, code and comments in English. Comments say what the code
  cannot; a docblock is one line by default, several only when each break falls
  where a reader pauses. Before handing over, list the comment blocks the diff
  adds that run over two lines, and every `//` pair.
- A claim about what a server accepts is **measured** on a container, through
  the real driver when it is about the driver — and on every schema of the
  server, not only the fixture database. Only `(dev)` connections and scratch
  databases (created and dropped in the same command).
- Every new test is checked by breaking what it covers (a mutation that must
  fail it). Test-only exports go through `export const testables = { … }`.
- `eslint --fix` only on explicitly named files, checking that it moved imports
  and nothing else. Never `prettier --write` a whole directory then restore
  files wholesale — commit 12 lost an edit that way.
- PR descriptions: one paragraph per line, no hard wrap.

## To check rather than assume

The project rule is that a claim about what a server accepts costs a container.
**No PG query in this plan has been run.** The six metadata queries go through
the verification schema of commit 15, output pasted into the PR. Also to
measure: that PG's enum labels fit the `listColumns` query itself
(`array_agg(e.enumlabel ORDER BY e.enumsortorder)` over `pg_enum`), which is
why a question is one statement. The three most likely to be wrong:

- `a.attnum = ANY (i.indkey::smallint[])` — `indkey` is an `int2vector`;
  fallback `array_position(i.indkey::smallint[], a.attnum) IS NOT NULL`.
- the interop shape of `pg` under Vite/Vitest (`{ default: {Client}, Client }`
  or `default` only) — a throwaway test before writing the driver.
- PG foreign keys: **do not go through `information_schema`**.
  `constraint_column_usage` carries no ordinal position, so a composite key
  `(a,b) → (x,y)` comes out as a 4-row cross product with no way to pair them
  back — the link would point at the wrong column, silently.
  `pg_constraint.conkey`/`confkey` are two parallel arrays that
  `unnest … WITH ORDINALITY` pairs exactly.

Two behaviour gaps to own and name in the PR: the structure page's `Key` column
is **synthesised** from `pg_index` (PG publishes no `COLUMN_KEY`) and will mark
every column of a composite index where MySQL marks only the first; and the
`pgsql` Monarch tokenizer colours a quoted identifier as a literal — not fixable
through the theme, since it is the same token name as a real string.

## Verification

At every commit: `yarn lint && yarn knip && yarn test`. After any commit
touching the main process or an IPC contract, **restart the app fully** — the
main process is not hot-reloaded.

Tests, the trap to avoid: `expect(sql).toBe(dialect.listTables('x').sql)` tests
nothing, and neither does a helper that rebuilds the expected text. So:

- **text** is asserted as a hand-written literal, in a file of its own per
  dialect. For the PG catalog queries, which run 30 lines, the whole query is
  not asserted — only markers that each **name a known bug**
  (`src.ord = tgt.ord` for pairing composite foreign keys).
- **invariants** are tested once, `describe.each` over the dialects, in
  `dialect.contract.test.ts` (done, commit 13): every statement binds exactly
  the parameters it names, is one statement and never interpolates a value it
  binds; `escapeIdentifier` throws on the empty string, doubles its own quote
  character and keeps a dotted name whole. The PG dialect is held to it as soon
  as its engine is registered (commit 20).
- the guarded write's **decision table** is tested per dialect, on hand-built
  fixtures: the two tables differ on purpose (PG decides on `RETURNING`, MySQL
  on `affectedRows` + `guardMatches`), and that gap is exactly what a reviewer
  should see.
- `src/sql/index.test.ts` tests **routing** (done for MySQL): each method sends
  a statement holding its dialect's marker (`INFORMATION_SCHEMA.X`). Commit 20
  adds a PG connection whose statements must hold `pg_catalog` — markers
  impossible in the other dialect.

In the app, against `tiana-dev-postgres` then `tiana-dev-mysql` for
non-regression: open the connection, move between schemas, open a wide table and
scroll, filter with `WHERE`, filter from a cell's context menu (including on a
value holding an apostrophe), follow a foreign-key link, open the structure
page, edit a cell of each type (text, number, date, `jsonb`, enum), force a
conflict by changing the row elsewhere before saving, run several statements in
the editor including a `CREATE FUNCTION … $$ … ; … $$` (which must stay **one**
statement), check completion and error underlining, draw a chart, and move from
a MySQL connection to a PG one **without restarting** to see the editor change
grammar.
